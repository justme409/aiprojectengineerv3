{
  "name": "projectpro-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "migrate": "pnpm --filter web db:migrate",
    "test-db": "node test-setup.js"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  }
}
